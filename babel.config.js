module.exports = {
  presets: [
    "@babel/preset-env",
    "@babel/preset-react"
  ],
  plugins: [
    '@babel/plugin-proposal-class-properties'
  ]
};

/**
 * @babel-preset-env -> é responsável por alterar as funcionalidades 
 * que o navegador ainda não entende.
 */

/**
 * @babel-present-react -> é responsável por alterar as funcionalidades
 * que o navegador ainda não entende do React.
 */
